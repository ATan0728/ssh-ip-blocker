# SSH IP Blocker

Программа для автоматической блокировки IP-адресов с подозрительными попытками входа по SSH.

## Установка

```bash
# Скачайте и установите RPM пакет
sudo dnf install ssh-ip-blocker-1.0-1.fc38.noarch.rpm
